/**
 * Re-exports from shared module — maintains backward compatibility.
 */
export { useBuilderStore } from '@repo/modules/website-builder/useBuilderStore';
export type { BuilderPage, BuilderSection, BuilderMode, BuilderState } from '@repo/modules/website-builder/useBuilderStore';
