/**
 * Re-exports from shared module — maintains backward compatibility for
 * any file that imports from '@/components/builder/SectionRenderer'.
 */
export { default, renderSection } from '@repo/modules/website-builder/SectionRenderer';
