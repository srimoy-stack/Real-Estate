import type { MLSProperty } from '../../listings-demo/types';

/**
 * DEPRECATED: Site is now fully integrated with live DDF API.
 * This file is kept only to maintain type stability in dev environments
 * but should no longer be referenced in production.
 */
export const MOCK_LISTINGS: MLSProperty[] = [];
